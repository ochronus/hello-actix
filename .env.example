# .env.example for hello-actix
#
# Copy this file to `.env` and adjust values as needed.
# The `.env` file is intended to be gitignored (already configured).

# -----------------------------------------------------------------------------
# Runtime mode: affects cookie security
# - prod: cookies are marked Secure (recommended default)
# - dev/test: cookies are NOT marked Secure
# -----------------------------------------------------------------------------
APP_MODE=prod

# -----------------------------------------------------------------------------
# Server settings
# -----------------------------------------------------------------------------
# TCP port to bind
APP_PORT=1337

# Session cookie name
APP_COOKIE_NAME=auth-example

# Session cookie TTL (seconds)
APP_COOKIE_TTL_SECONDS=300

# -----------------------------------------------------------------------------
# Secret key (REQUIRED FOR PROD)
# -----------------------------------------------------------------------------
# The secret key secures session cookies. In production, you MUST set a stable,
# cryptographically strong key. It must decode to at least 64 bytes.
#
# Provide one of the following forms:
#   APP_SECRET_KEY=base64:<BASE64_STRING>
#   APP_SECRET_KEY=hex:<HEX_STRING>
#
# If omitted, a random key is generated at startup (OK for dev/test; NOT for prod).
#
# Examples to generate a key:
#   # Base64 (Linux/macOS):
#   openssl rand -base64 64
#   # or:
#   head -c 64 /dev/urandom | base64
#
#   # Hex:
#   openssl rand -hex 64
#
# Uncomment and paste your generated key below:
# APP_SECRET_KEY=base64:PASTE_YOUR_BASE64_KEY_HERE

# -----------------------------------------------------------------------------
# Optional legacy port override (some PaaS set this automatically)
# If set, this overrides APP_PORT.
# -----------------------------------------------------------------------------
# PORT=1337

# -----------------------------------------------------------------------------
# Optional logging
# -----------------------------------------------------------------------------
# RUST_LOG=info,actix_web=info
