[package]
edition = "2024"
name    = "hello-actix"
version = "0.1.0"

[dependencies]
actix-session = { version = "0.11.0", features = [
  "cookie-session",
  "redis-session",
  "redis-session-native-tls",
  "redis-pool",
] }
actix-web = "4"
